`use strict`;

import './main.scss';
// import fundamentals_1 from './playground/fundamentals-1.js';
// fundamentals_1();

/** Output
 * Window object;
 * Console object;
 */
// import output from './modules/output.js';
// output();

/** Variables
 * undefined DT;
 * null DT;
 * Hoisting;
 * Scopes;
 */
// import variables from './modules/variables.js';
// variables();

/** Data Types
 * Constructors;
 * Primitive.prototypes;
 * typeof operator;
 * Primitive & Reference - Stack vs. Heap;
 */
// import datatypeof from './modules/data-typeof.js';
// datatypeof();

/** Number
 * Math.object();
 * Operators: Arithmetic, Increment/Decrement;
 * BigInt DT;
 */
// import number from './modules/dt-number.js';
// number();

/** String
 * Concatenation;
 * Iteration: for...of;
 */
// import string from './modules/dt-string.js';
// string();

/** Boolean
 * Truthy & Falsy values;
 * Comparison operators;
 */
// import boolean from './modules/dt-boolean.js';
// boolean();

/** Symbol
 * Variable chaining;
 */
// import symbol from './modules/dt-symbol.js';
// symbol();

/** RegExp
 * Cheatsheet
 */
// import regexp from './modules/dt-regexp.js';
// regexp();

/** Date
 * Date-fns library;
 */
// import date from './modules/dt-date.js';
// date();

/** Conditionals
 * if / else statements;
 * Operators: Ternary, Logical, Nullish Coalescing;
 * switch statement;
 */
// import conditionals from './modules/conditionals.js';
// conditionals();

/** Loops
 * while;
 * do...while;
 * for;
 * Infinite loops;
 * Keywords: break, continue;
 */
// import loops from './modules/loops.js';
// loops();

/** Functions
 * Declaration;
 * Expression;
 * Parameters & Arguments;
 * Operators: Spread, Rest;
 * Arrow;
 * Callback;
 * IIFE;
 * Closures;
 * Recursion;
 */
// import functions from './modules/dt-function.js';
// functions();

/** Array
 * Spread operator;
 * Iterations
 * ArrayBuffer
 * Set
 * WeakSet
 */
// import array from './modules/dt-array.js';
// array();

/** Object
 * Deep copy: Lodash, JSON.parse(JSON.stringify());
 * Iterations: for...in, Object.entries(), Object.keys(), Object.values();
 * Map
 * WeakMap
 */
// import object from './modules/dt-object.js';
// object();

/** OOP
 * Constructor function;
 * Prototypes;
 * Classes;
 * Inheritance;
 * Static methods;
 * Getters & Setters;
 * Private fields;
 * Mixins;
 */
// import oop from './modules/oop.js';
// oop();

/** Async
 * Event Loop: setTimeout(), setInterval();
 * Callbacks;
 * Promises;
 * Async/Await;
 * Microtasks & Macrotasks;
 * CORS;
 * HTTP methods;
 * AJAX;
 * XMLHttpRequest;
 * JSON;
 * Fetch API;
 * REST API;
 * Axios;
 */
// import async from './modules/async.js';
// async();

/** DOM
 * Selectors;
 * Attributes;
 * Manipulation;
 * Events;
 * Event bubbling & capturing;
 * Event delegation;
 * Event propagation;
 * Event listeners;
 * Event object;
 * Event handlers;
 * Event loop;
 * Event queue;
 * Event stack;
 */
// import dom from './modules/api-dom.js';
// dom();

/** BOM
 * Window object;
 * Location object;
 * Navigator object;
 * Screen object;
 * History object;
 * Cookies;
 * LocalStorage;
 * SessionStorage;
 * Web Workers;
 * Service Workers;
 * Web Sockets;
 */
// import bom from './modules/api-bom.js';
// bom();

/** TS
 * Types;
 * Interfaces;
 * Generics;
 * Classes;
 * Enums;
 * Decorators;
 * Modules;
 * Namespaces;
 * Namespaces vs. Modules;
 * Triple-slash directives;
 * Type assertions;
 * Type guards;
 * Type aliases;
 * Type inference;
 * Type compatibility;
 * Type erasure;
 * Declaration merging;
 * Ambient declarations;
 * Declaration files;
 * Utility types;
 * Intersection types;
 * Type casting;
 * Type assertions;
 * Literal types;
 * Discriminated unions;
 * Index types;
 * Mapped types;
 * Conditional types;
 */
// import typescript from './modules/typescript.ts';
// typescript();